/**
 * Re-export AIUnifiedService as AIService for backward compatibility
 * This allows existing code to continue working without changes
 */
export { AIUnifiedService as AIService } from './ai-unified.service';
